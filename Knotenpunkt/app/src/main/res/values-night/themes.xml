<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Knotenpunkt" parent="Theme.AppCompat.DayNight.NoActionBar"> <!-- MaterialComponents.DayNight.NoActionBar -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.MainActivity" parent="Theme.AppCompat.DayNight.NoActionBar"> <!-- Theme.MaterialComponents.Light.NoActionBar -->
        <item name="android:windowBackground">@drawable/mainactivity_background</item>
    </style>
</resources>